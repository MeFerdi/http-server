[package]
name = "localhost"
version = "0.1.0"
edition = "2021"
description = "A single-threaded, non-blocking HTTP/1.1 server built on epoll"
authors = []

[[bin]]
name = "localhost"
path = "src/main.rs"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
strip = true
